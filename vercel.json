{
  "buildCommand": "echo 'No build needed'",
  "outputDirectory": ".",
  "devCommand": "python -m http.server 3000",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/SafeTrace AI.html"
    }
  ]
}
